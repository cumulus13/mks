name: Release

on:
  push:
    tags:
      - 'v*'

env:
  CARGO_TERM_COLOR: always

permissions:
  contents: write
  pull-requests: write

jobs:
  create-release:
    name: Create Release
    runs-on: ubuntu-latest
    outputs:
      upload_url: ${{ steps.create_release.outputs.upload_url }}
      release_id: ${{ steps.create_release.outputs.id }}
    steps:
    - uses: actions/checkout@v4
    
    - name: Create Release
      id: create_release
      uses: softprops/action-gh-release@v1
      with:
        draft: false
        prerelease: false
        generate_release_notes: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  build-release:
    name: Build Release
    needs: create-release
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            binary1_name: mks
            asset_prefix: linux-amd64
          - os: ubuntu-latest
            target: x86_64-unknown-linux-musl
            binary1_name: mks
            asset_prefix: linux-musl-amd64
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            binary1_name: mks.exe
            asset_prefix: windows-amd64
          - os: macos-latest
            target: x86_64-apple-darwin
            binary1_name: mks
            asset_prefix: macos-amd64
          - os: macos-latest
            target: aarch64-apple-darwin
            binary1_name: mks
            asset_prefix: macos-arm64

    steps:
    - uses: actions/checkout@v4
    
    - name: Install Rust
      uses: dtolnay/rust-toolchain@stable
      with:
        targets: ${{ matrix.target }}

    - name: Install musl-tools (Linux musl)
      if: matrix.target == 'x86_64-unknown-linux-musl'
      run: sudo apt-get update && sudo apt-get install -y musl-tools

    - name: Build release
      run: cargo build --release --target ${{ matrix.target }}

    - name: Strip binaries (Linux/macOS)
      if: matrix.os != 'windows-latest'
      run: |
        strip target/${{ matrix.target }}/release/${{ matrix.binary1_name }}

    - name: Create tarball (Linux/macOS)
      if: matrix.os != 'windows-latest'
      run: |
        cd target/${{ matrix.target }}/release
        tar czf ../../../mks-${{ matrix.asset_prefix }}.tar.gz ${{ matrix.binary1_name }}

    - name: Create zip (Windows)
      if: matrix.os == 'windows-latest'
      shell: pwsh
      run: |
        cd target/${{ matrix.target }}/release
        Compress-Archive -Path ${{ matrix.binary1_name }} -DestinationPath ../../../mks-${{ matrix.asset_prefix }}.zip

    - name: Upload Release Assets
      uses: softprops/action-gh-release@v1
      with:
        files: |
          mks-${{ matrix.asset_prefix }}.tar.gz
          mks-${{ matrix.asset_prefix }}.zip
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  publish-crate:
    name: Publish to crates.io
    needs: build-release
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
    - uses: actions/checkout@v4
    
    - name: Install Rust
      uses: dtolnay/rust-toolchain@stable

    - name: Publish to crates.io
      run: cargo publish --token ${{ secrets.CARGO_REGISTRY_TOKEN }}
      continue-on-error: true
